(list (simple "echo" "$x" "${y}" "${#n}" "$1" "$@" "$*" "$#" "$?" "$$" "$!" "$-"))
(list (simple "echo" "${z:-def}" "${z-d}" "${z:=as}" "${z=a}" "${z:?msg}" "${z?m}" "${z:+alt}" "${z+a}"))
(list (simple "echo" "${p#a*}" "${p##a*}" "${p%z}" "${p%%z}" "\"${v}post\"" "\"pre${v:-x}\""))
